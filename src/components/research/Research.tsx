import React from "react";

const Research = () => {
  return (
    <section className="custom-screen">
      <div className="flex flex-col md:flex-col max-w-3xl mx-auto ">
        <h1 className="text-3xl font-bold">Research</h1>
        <p className="text-md text-gray-400 mt-2">In progress...</p>
      </div>

      {/* researchs */}
      {/* - detecting and classifying plant diseases using image data
      - movie recommendation system using collaborative filtering
      - predicting stock prices using LSTM
      - predicting house prices using linear regression
      - predicting heart disease using neural networks
      - predicting breast cancer using neural networks */}

    </section>
  );
};

export default Research;
